@model BurgerViewModel

<div>
    <img src="@Model.ImageUrl" alt="@Model.Name" class="rounded col-4"/>
    <span class="col-6">
        <h3>@Model.Name</h3>
        <h4>@Model.Description</h4>
    </span>
</div>
